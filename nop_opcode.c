#include "monty.h"

/**
 * nop - entry point
 * @stack: pointer to a poiter to the stack
 * @line_number: line number of te current opcode
 */

void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
